version: '3.5'
services:
  app:
    build:
      context: .
      dockerfile: DockerfilePhpFpmWithNginx
    image: sheba.xyz/php-fpm-7.0.33-with-nginx
    container_name: app
    restart: unless-stopped
    tty: true
    environment:
      SERVICE_NAME: app
      DB_HOST: sheba-development.cifg1gfqorjf.ap-south-1.rds.amazonaws.com
      DB_DATABASE: sheba_fresh
      REDIS_HOST: sheba-dev.psiyqa.ng.0001.aps1.cache.amazonaws.com
    working_dir: /var/www
    ports:
      - "9096:80"
    volumes:
      - ./:/var/www
      - ./php/local.ini:/usr/local/etc/php/conf.d/local.ini
    networks:
      - app-network

  # webserver:
  #   image: nginx:alpine
  #   container_name: webserver
  #   restart: unless-stopped
  #   tty: true
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./:/var/www
  #     - ./nginx/conf.d/:/etc/nginx/conf.d/
  #   networks:
  #     - app-network

networks:
  app-network:
    driver: bridge
    # external:
    #   name: sheba-docker_development_network
